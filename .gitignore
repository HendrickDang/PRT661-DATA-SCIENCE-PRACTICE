# dataset/raw/, dataset/processed/, eda_plots/ and regression_plots/ are all
# committed on purpose, so teammates and markers can read the outputs without
# running the pipeline. They are still regenerated on every run - edit the
# pipeline, not these files.

# Office lock files and drawio backups
~$*
.~lock.*
*.bkp
.$*.bkp

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/

# Environment / editor
.env
.vscode/
.idea/
.DS_Store
Thumbs.db
